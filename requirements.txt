# setuptools: not bundled in Python 3.12 venvs by default; crewai's telemetry
# module imports pkg_resources at import time and breaks without it. Pinned
# below 81 because newer setuptools releases dropped pkg_resources entirely.
setuptools==75.6.0

# Core web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Settings / env
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.3.0

# Database (async SQLAlchemy + Postgres - relational data only)
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Vector store (Qdrant - one collection per Organization, see docs/05-data-model.md)
qdrant-client==1.12.1

# Async task queue
celery[redis]==5.4.0
redis==5.2.1

# Object storage
boto3==1.35.90

# Resume text extraction (E6) - pinned to the version already resolved
# transitively by crewai's dependency graph.
pypdf==6.14.2

# Auth (JWT validation against managed provider per docs/07-technical-stack.md)
pyjwt[crypto]==2.10.1
httpx>=0.27.0,<0.28.0
# Pinned below 0.28 to satisfy litellm's httpx<0.28 constraint (transitive, via crewai).

# AI providers
anthropic==0.42.0
voyageai==0.3.1
crewai==0.86.0
litellm==1.55.3

# Speech-to-text fallback for interview audio recordings without a
# platform-provided transcript (see vector.md) - OpenAI Whisper is a
# swappable default, not a final vendor decision (docs/07-technical-stack.md).
# Pinned to the version already resolved transitively by crewai's
# `instructor` dependency (which requires openai>=2.0.0) rather than an
# independently-chosen version, to avoid a resolver conflict.
openai==2.45.0

